# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
.obsidian/
*.swp
*.swo
*~
.DS_Store

# AI Employee - Sensitive & Temporary
# Inbox contains raw dropped files - process and remove
Inbox/*
# Logs may contain sensitive info (encrypt before commit)
Logs/*.json
# Sensitive folders
Pending_Approval/
Approved/
Rejected/
credentials.json         # API credentials - NEVER commit
token.json              # OAuth tokens - NEVER commit
secrets.env              # Environment file with secrets
.env                     # Environment file
.session/                # Browser sessions (WhatsApp, etc.)
linkedin_session/        # LinkedIn browser session (cookies, auth)
mcp-config.json          # MCP server configs (may contain tokens)

# MCP Servers - Gold Tier
mcp-servers/*/.env       # Odoo/social media API keys
mcp-servers/odoo-mcp/odoo_data/  # Odoo persistence (Docker)
mcp-servers/odoo-mcp/postgres_data/  # PostgreSQL data

# Health Monitor - Phase 3
utils/error_recovery/health_server.pid  # PID file
utils/error_recovery/health_server.log  # Health monitor logs

# Operating System
Thumbs.db
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Desktop.ini

# Testing
.pytest_cache/
coverage.xml
htmlcov/

# Backup files
*.bak
*.backup
*.old

# Drop folders
drops/
drop/
watch/
incoming/

# Logs
*.log
logs/
